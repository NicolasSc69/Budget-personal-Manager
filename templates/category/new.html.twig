{% extends 'base.html.twig' %}

{% block title %}{{ 'New tag'|trans }}{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">{{ 'New tag'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            {{ include('category/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
