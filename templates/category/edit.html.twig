{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit'|trans }} {{ category.name }}{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">{{ 'Edit tag'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <div class="mt-3">
        {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
