{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit'|trans }} {{ account_type.name }}{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">{{ 'Edit account type'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            {{ include('account_type/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <div class="mt-3">
        {{ include('account_type/_delete_form.html.twig') }}
    </div>
{% endblock %}
