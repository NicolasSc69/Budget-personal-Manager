{% extends 'base.html.twig' %}

{% block title %}{{ 'New account type'|trans }}{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">{{ 'New account type'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            {{ include('account_type/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
