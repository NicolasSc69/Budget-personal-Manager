{% extends 'base.html.twig' %}

{% block title %}{{ 'New account'|trans }}{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">{{ 'New account'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            {{ include('account/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
