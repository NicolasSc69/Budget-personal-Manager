{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit'|trans }} {{ account.label }}{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">{{ 'Edit account'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            {{ include('account/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <div class="mt-3">
        {{ include('account/_delete_form.html.twig') }}
    </div>
{% endblock %}
