{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit'|trans }} {{ person.name }}{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">{{ 'Edit person'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            {{ include('person/_form.html.twig', {'button_label': 'Update'|trans}) }}
        </div>
    </div>

    <div class="mt-3">
        {{ include('person/_delete_form.html.twig') }}
    </div>
{% endblock %}
