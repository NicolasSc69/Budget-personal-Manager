{% extends 'base.html.twig' %}

{% block title %}{{ 'New transaction'|trans }}{% endblock %}

{% block body %}
    <h1 class="h3 mb-3">{{ 'New transaction'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            {{ include('transaction/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
